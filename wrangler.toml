name = "kalmate"
compatibility_date = "2024-11-01"

pages_build_output_dir = ".output/public"

[vars]
# GROQ_API_KEY set in Cloudflare Dashboard (Settings > Environment Variables)

[ai]
binding = "AI"
# Cloudflare Workers AI — fallback pipeline generator when GROQ_API_KEY isn't
# set or Groq's request fails. Free tier: 10,000 Neurons/day, no card needed.
